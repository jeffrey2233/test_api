# Core testing framework
pytest>=7.0.0

# HTTP client for API calls
requests>=2.30.0

# Allure integration for pytest
allure-pytest>=2.0.0b1

# Optional: parallel test execution to speed up test runs
pytest-xdist>=3.0.0

# Optional: load environment variables from .env files
python-dotenv>=1.0.0